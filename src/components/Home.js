import Alert from 'react-bootstrap/Alert';

const Home = () => {
  return (
    <div className="App">
      <Alert variant='primary'>
          Home
      </Alert>
    </div>
  );
}

export default Home;
